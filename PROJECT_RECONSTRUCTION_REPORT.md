# 两份工程融合与优化说明

## 1. 审计对象

- 简单版：`GovEnt-AgentShield(5).zip`，源码链路紧凑，包含Git历史、较完整的运行历史和10个人工Skill样例。
- 全面版：`GovEnt-AgentShield 11(2).zip`，增加认证、RBAC/ABAC、任务链、审批、受控执行、哈希审计、API、Docker和发布验收，但压缩包误带约445MB Windows虚拟环境。
- 任务书：题目编号`XA-202620`，要求围绕多源输入、工具执行、供应链和评测审计提交可复现原型。

## 2. 两版优点保留情况

| 来源 | 优点 | 本项目处理 |
|---|---|---|
| 简单版 | 10个人工Skill行为样例，覆盖普通读取、敏感读取、日志追加、外传、删除和Shell注入 | 完整保留到`data/skill_cases/curated/`，增加四级动作标签和包级评测 |
| 简单版 | Skill Scanner能区分敏感文件与合法日志追加，关注`shell=True`上下文 | 与全面版更多危险API合并，恢复细粒度上下文和低误报逻辑 |
| 简单版 | Git历史可追踪团队阶段成果 | 不把`.git`打进交付包；把必要的设计与责任信息转写到文档 |
| 全面版 | 认证、权限、审批、执行和审计形成强制闭环 | 作为最终主干保留并继续加固 |
| 全面版 | 一键验收、Dashboard、API、Docker和精选证据 | 保留并扩展到多源与供应链评测 |
| 全面版 | 320条输入集与工具/Skill生成回归 | 保留，同时明确它们不是外部盲测 |

## 3. 原工程主要问题

1. 多源输入只在关键词规则中出现，没有来源信封、逐源检测、内容指纹和跨边界控制。
2. Skill安全以单个Python文件为主，无法检查依赖源、ZIP穿越/炸弹、敏感材料、二进制载荷和包清单。
3. 全面版Skill扫描扩大API覆盖时，丢失了简单版的`shell=True`、敏感文件和日志追加细分。
4. 会话状态未绑定用户，其他用户理论上可复用同一`session_id`。
5. 已批准请求过期后仍可消费；文件状态操作只有单次逻辑，没有进程内互斥。
6. API把字符串`"false"`通过`bool()`转为真，且没有请求体大小和对象类型校验。
7. 审计写入只`flush`未`fsync`，进程或系统异常时证据耐久性较弱。
8. Docker默认root运行、可写根文件系统，缺少最小权限配置。
9. 大包误带完整虚拟环境，小包误带`.git`和大量开发历史输出，不适合作为最终代码仓库。

## 4. 本轮新增与修正

- 新增`MultiSourceGuard`：来源类型、来源地址、可信度、数据密级、大小限制、逐源检测、SHA-256指纹、脱敏证据和跨边界阻断。
- 修正运行顺序：单源与多源共同形成输入总门禁；`review/block`立即短路，Skill、Tool、Policy、Task Chain、Registry和Execution均不进入。
- 增加跨来源共享实体图、敏感/传输/绕过/持久化分散信号组合，以及受限Base64分片重组。
- 新增`SupplyChainGuard`：目录/ZIP/单文件无执行扫描、AST、依赖、归档、敏感材料、二进制载荷、文件清单和包哈希。
- 把多源最高密级并入RBAC/ABAC有效密级，客户端不能降级来源数据。
- 任务链绑定首次用户；审批增加获批后到期校验和进程内互斥；API严格要求JSON布尔值并限制请求体。
- 审计落盘增加`fsync`；Docker改为非root、只读根文件系统、丢弃capabilities并启用健康检查。
- Dashboard新增“多源输入与供应链准入”页，受控执行页支持`input_sources`。
- Dashboard新增默认“一键安全检测”页，16条案例直接显示结论和每一阶段是否进入。
- 增加多源、供应链和安全加固测试，新增两个评测脚本并接入一键发布门禁。
- 增加威胁模型、任务书对齐、技术方案报告、演示脚本、CI和Docker忽略文件。

## 5. 交付清理

最终包不包含`.venv`、`.git`、`__pycache__`、开发期`output/`和临时日志。运行证据由一键验收重新生成，精选结果保存到`release_evidence/`。这样既保留复现能力，也避免把环境、个人路径、历史任务文本或无用缓存提交到仓库。

## 6. 仍需团队完成的事项

- 录制不超过10分钟的真实演示视频；
- 制作规则冻结后的独立盲测集；
- 本轮已在全新 Python 3.12 虚拟环境执行完整验收：139/139 测试通过，16/16 一键案例、API健康检查和Streamlit AppTest均通过，顶层状态为 `passed`；真实结果见 `release_evidence/verification_report.json`；正式提交机器仍应再次复验环境兼容性；
- 本轮已用冻结验收结果替换 `release_evidence/` 精选证据；
- 填写学校、团队、负责人和报名信息；
- 如接入真实政企业务，替换演示身份、文件审批存储、本地审计和Demo工具。
